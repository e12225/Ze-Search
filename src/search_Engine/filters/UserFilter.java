package search_Engine.filters;

import org.json.simple.JSONArray;

public class UserFilter {

    public JSONArray getUsers(String field, Object value, JSONArray userJSON)
    {
        JSONArray users = new JSONArray();
        return users;
    }
}
